# ============================================================================
# Project 2 - HTTP + HTTPS Example
# ============================================================================
# This project demonstrates:
# - Mode 1: Access via .localhost (local development only)
# - Mode 3: Access via custom domain with HTTPS (LAN-accessible with SSL)
# ============================================================================

# Mode 1: Local development domain (accessible only from this computer)
# Usage: http://project2.localhost
APP_DOMAIN=project2.localhost

# Mode 3: Custom domain with HTTPS support (accessible from any device on your network)
# Usage: https://project2-192-168-1-10.example.com
# Replace: project2 with your project name
#          192-168-1-10 with your local IP in dashed format (e.g., 192.168.1.10 → 192-168-1-10)
#          example.com with your custom domain registered at account.devns.me
APP_DOMAIN_LAN=project2-192-168-1-10.example.com

# Traefik network (must match the network created by traefik/docker-compose.yml)
PROXY_NETWORK=traefik_network
