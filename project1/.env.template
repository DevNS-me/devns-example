# ============================================================================
# Project 1 - HTTP Only Example
# ============================================================================
# This project demonstrates:
# - Mode 1: Access via .localhost (local development only)
# - Mode 2: Access via .devns.me (LAN-accessible via HTTP)
# ============================================================================

# Mode 1: Local development domain (accessible only from this computer)
# Usage: http://project1.localhost
APP_DOMAIN=project1.localhost

# Mode 2: LAN-accessible domain (accessible from any device on your network)
# Usage: http://project1-192-168-1-1.devns.me
# Replace: project1 with your project name
#          192-168-1-1 with your local IP in dashed format (e.g., 192.168.1.100 → 192-168-1-100)
APP_DOMAIN_LAN=project1-192-168-1-1.devns.me

# Traefik network (must match the network created by traefik/docker-compose.yml)
PROXY_NETWORK=traefik_network
