PROXY_NETWORK=traefik_network

# ============================================================================
# HTTPS Certificates (DevNS.me)
# ============================================================================
# To enable HTTPS for your projects:
# 1. Register your custom domain at https://account.devns.me
# 2. Get the certificate and private key URLs from your DevNS.me dashboard
# 3. Add them below as HTTPS_CERT_URL_X and HTTPS_KEY_URL_X pairs
# 4. Restart Traefik: docker compose down && docker compose up -d
#
# You can add as many certificate pairs as you need - just increment the number.
# The system will automatically detect and configure all certificate pairs.
# ============================================================================

# Example certificate pair 1 (uncomment and replace {TOKEN} with your actual token)
#HTTPS_CERT_URL_1=https://cert.devns.me/{TOKEN}/certificate.crt
#HTTPS_KEY_URL_1=https://cert.devns.me/{TOKEN}/private.key

# Example certificate pair 2
#HTTPS_CERT_URL_2=https://cert.devns.me/{TOKEN}/certificate.crt
#HTTPS_KEY_URL_2=https://cert.devns.me/{TOKEN}/private.key

# Add more certificate pairs as needed:
#HTTPS_CERT_URL_3=https://cert.devns.me/{TOKEN}/certificate.crt
#HTTPS_KEY_URL_3=https://cert.devns.me/{TOKEN}/private.key

# Note: No need to modify docker-compose.yml or tls.yml - everything is automatic!
